#include <iostream>

using namespace std;

int main()
{
    /// auto means it will detact automatic datatype
    /// to enable c++ 11 go to setting>compiler>comiler setting(make sure that GNU GCC Compiler is set)>>compiler flags>c++ 11  standard if u scroll
    auto a = 42;
    auto b = 12.4;
    return 0;
}
