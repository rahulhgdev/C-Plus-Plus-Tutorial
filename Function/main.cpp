#include <iostream>

using namespace std;

void myFunc(){
    cout<<"We are in Function"<<endl;
}
int main()
{
    /// definition - it is block of code which execute something ideally a  function performs a specific task
    /// function is used to reduce the size of code
    /// syntax -  (function return datatype)datatype function-name(arguments or parameters ){statemenst}
    /// to call function
    myFunc();
    return 0;

}
